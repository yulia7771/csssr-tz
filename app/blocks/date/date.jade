mixin date()
	+b.date&attributes(attributes)

		- var date = new Date();
		- var m =  date.getMonth()+1;
		- m = m < 10 ? '0' + m : m;
		- var d = date.getDate();
		- d = d < 10 ? '0' + d : d;
		- var dateStr = `${d}.${m}.${date.getFullYear()}`;

		+e.text Дата заполнения
		+e.value= dateStr
